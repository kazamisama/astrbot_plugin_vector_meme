# vector_meme 0.7.2 交接文档

## 交接信息

- 交接日期：2026-08-09
- 交接对象：`astrbot_plugin_vector_meme`
- 当前版本：0.7.2（`main.py` 注册版本、`metadata.yaml`、CHANGELOG 三者一致）
- 当前 HEAD：`ebdfd3b`（含 0.7.2 配置清理）
- 远端状态：本次交接文档提交后推送至 `origin/main`
- 工作区状态：干净

## 任务背景

本次工作分三个阶段：

1. 全量源码审查：读完 `main.py`、`core/`、`scripts/`、`utils/` 与配置 schema，确认 3 个确定 bug、4 个 P2 风险、若干 P3 清理项。
2. grill 决策：确定“全量修复清单”，方案按 4 个提交实施，最后补 1 个 docs 提交。
3. 实施与验证：pytest 全绿（15 passed）、全仓 py_compile 通过、`.cache` 索引健康检查通过。
4. 0.7.2 配置清理：删除 `trigger_probability`、`_temp_dim` 内部化、随机控制文档化。

## 已确认事实

- 当前图库：`memes/` 17 个标签、191 张图片，`memes/captions.json` 存在且纳入 git 跟踪。
- 离线索引 `.cache/memes.db` + `.cache/memes.faiss`：`health_check ok=True`，`total=191`，`index_size=191`，`orphan_vector_count=0`。
- FAISS 维度：512（与 `ViT-B-32` 对应）。
- 测试环境：Python 3.12.13，`faiss 1.15.0`，`pytest 9.1.1`；宿主 Python 无法直接 `import astrbot`，测试通过 conftest 中的最小 astrbot 桩导入 `main.py`。
- 真实 AstrBot 配置文件 `astrbot_plugin_vector_meme_config.json` 带 UTF-8 BOM 且已是分组结构，迁移脚本已兼容读取。

## 提交清单

| 提交 | 内容 |
|---|---|
| `4a1ebb2` | 重建保护、维度残留、迁移脚本修复 |
| `284838c` | FAISS 全局锁、进度队列线程安全、命中后取行 |
| `a22f20d` | persona marker、孤儿向量压缩、绝对路径、死代码清理 |
| `352af10` | pytest 测试、dual 检索二次索引修复、版本 0.7.1 |
| `f090b27` | README 重写对齐 0.7.1 |
| `ebdfd3b` | 0.7.2 配置清理：删除 trigger_probability、_temp_dim 内部化、配置一致性测试 |

## 已修改内容

### 1. 重建安全（`main.py`）

- `/vm 重建` 顺序改为：先备份 `memes.db` / `memes.faiss` 为 `.bak.v071` → 以 `for_rebuild=True` 加载 embedder → 成功后才清库重建 → 成功删除备份。
- embedder 加载失败或索引进程失败时，旧库与备份保留，并在回复中提示恢复方法。
- `_ensure_embedder(for_rebuild=True)` 跳过维度保护；空库 + 残留旧维度 FAISS 时先删除残留索引再按新维度建库。

### 2. FAISS 并发与检索（`core/database.py`、`core/retriever.py`、`core/retriever_dual.py`）

- `add_vectors`、`search`、`save_index`、`index_size` 统一持锁。
- 新增带锁接口：`search_index()`、`reconstruct()`、`reconstruct_all()`、`reset_index()`。
- 检索改为 FAISS 命中后只加载命中的 meme 行，去掉全量预取和超长 `IN` 子句。
- 修正 `IndexFlatIP + IDSelector` 仍是全量扫描、只过滤结果集的注释。

### 3. persona 与维护（`main.py`、`core/database.py`、`core/indexer.py`）

- persona 注入改为 marker 块幂等方案（`PERSONA_INJECT_RE`），移除一次性 deep copy 备份与位置 zip 依赖。
- `/vm 修复` 增加孤儿向量压缩：`index_size > live_vector_count + max(50, 20% × live_vector_count)` 时触发 `compact_index()`。
- `compact_index()` 保留 disabled 行仍被引用的向量，重映射 `vector_id` / `caption_vector_id` 并保存索引。
- 索引路径统一 `resolve()` 存绝对路径。
- 删除死代码：`rebuild_index_from_db()`、`_strip_placeholders()`。

### 4. 0.7.2 配置清理（`main.py`、`_conf_schema.json`、`tests/`）

- 删除 `trigger_probability` 配置与整条消息随机丢弃分支：LLM 输出标签后直接检索发送，旧配置残留静默失效。
- `_temp_dim` 隐藏配置改为模块常量 `INIT_TEMP_DIM = 512`，不再从配置文件读取。
- `selection_random_jitter` 与 `enable_stochastic_selection` 分工写进 schema hint 与 README：前者只影响排序扰动，后者控制是否随机选图。
- 新增 AST 配置一致性测试，确保 `config.get(...)` 键全部存在于 schema，且 `trigger_probability` / `_temp_dim` 不再出现。

### 5. 测试与版本

- 新增 `tests/`，pytest 为 dev-only（`tests/requirements-dev.txt`），不进入 `requirements.txt`。
- `main.py`、`metadata.yaml`、`CHANGELOG.md` 当时统一升到 0.7.1，后由 0.7.2 提交继续升至 0.7.2。
- 修复 dual 检索对已展平结果二次 `[0]` 索引的回归。

## 关键代码入口

- 插件入口：`main.py`
- SQLite + FAISS 存储：`core/database.py`（`search_index`、`compact_index`、`live_vector_count`、`reset_index`）
- 检索：`core/retriever.py`、`core/retriever_dual.py`
- 索引：`core/indexer.py`
- 嵌入后端：`core/embedder.py`
- caption：`core/captioner.py`
- 外部 sticker 接口：`main.py::search_sticker_for_external`（30 秒超时、冷启动懒加载 embedder、不调用 `pick()`、失败返回 `None`）

## 已验证

- `python -m pytest tests -q`：15 passed（含新增配置一致性测试）。
- 全仓 `py_compile`：全部 Python 文件通过。
- `.cache` 索引健康检查：`ok=True`，191/191，orphan=0。
- 迁移脚本在临时副本上端到端通过（含 BOM 配置文件），未触碰真实配置。
- 孤儿压缩功能验证：删除一条 meme 后 orphan 计数正确，`compact_index()` 重映射后检索 id 有效、orphan=0。

## 未验证与风险

- AstrBot 真机运行时：流式补发（`STREAMING_FINISH`）、persona 注入到真实 provider、`/vm 重建` / `/vm 修复` 完整命令链路。
- 0.7.2 行为变化：删除概率分支后“LLM 输出标签即发送”的真机表现未实测。
- 并发场景：索引进行中同时触发检索的真实线程行为。
- 大库压力：超过 SQLite 变量上限的候选规模未实测，命中后取行已从结构上消除该上限。
- 模型下载 / 换模型维度路径：未实测。
- `compact_index()` 保留 disabled 向量是设计选择：重新启用 disabled 表情不需要重新索引；健康检查的 orphan 指标按“DB 引用向量数”计算，disabled 向量不计为孤儿。
- 测试环境用 astrbot 桩而非真实 AstrBot 运行时，钩子层行为只能靠真机确认。

## 备份与恢复

### 重建备份（`.bak.v071`）

- 位置：与 `memes.db` / `memes.faiss` 同目录，文件名 `memes.db.bak.v071`、`memes.faiss.bak.v071`。
- 成功重建后自动删除；失败时保留。
- 恢复方法：停止 AstrBot，删除 `memes.db` / `memes.faiss`，把 `.bak.v071` 改回原名后重启。

### 代码编辑备份

代码修改均通过 safe-edit 完成，自动备份在：

```text
C:\Users\chiriu\.irmia\backups\
```

可用 `safe_edit list-backups <file>` 查看，`safe_edit rollback <file>` 回滚。

## 测试与常用命令

```bash
pip install -r tests/requirements-dev.txt
python -m pytest tests -q
python -m py_compile main.py core/*.py utils/*.py tests/*.py
```

离线建索引：

```bash
python scripts/build_openclip_index.py --meme-dir memes --data-dir .cache
```

健康检查：

```text
/vm 状态
/vm 诊断
/vm 修复
```

## 已排除路径

- FAISS 并发未采用读写锁，选择全局锁（当前库规模无性能压力）。
- 孤儿向量未做“删除即压缩”，统一由 `/vm 修复` 按阈值压缩。
- pytest 未进入插件运行时依赖。
- 未做 AstrBot event / LLM 钩子层的 mock 级测试。

## 下一步建议

1. 在真实 AstrBot 实例上按顺序验证：`/vm 状态` → `/vm 预热` → `/vm 重建` → `/vm 修复` → 一条 `%%tag%%` 自动回复 → `<sticker>` 转发。
2. 验证通过后打 `v0.7.2` tag 并发布 release notes。
3. 如需继续优化，评估大库下索引/检索压测，以及删除路径是否值得做即时回收。
4. 交接文档本身按需提交；当前未纳入 git。
